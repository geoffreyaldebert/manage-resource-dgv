version: "3"
services:
  redis:
    image: redis
    ports:
      - "6380:6379"
  mongodb:
    image : mongo
    entrypoint: [ "/usr/bin/mongod", "--bind_ip", "0.0.0.0"]
    container_name: mongodb
    volumes:
    - ./mongo/:/data/db
    ports:
      - "27020:27017"
    restart: unless-stopped
